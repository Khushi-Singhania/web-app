on: push
jobs:
  build-container:
    name: Build Container
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-activity@v1
        with:
          username: ${{secrets.DOCKERHUB_USER}}
          paassword: ${{ secrets.DOCKERHUB_TOKEN}}
          
